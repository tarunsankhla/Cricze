<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cricze">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlayersActivityProfile"></activity>
        <activity android:name=".TeamsActivityProfile" />
        <activity android:name=".TournamentsActivity" />
        <activity
            android:name=".NotificationActivity"
            android:launchMode="singleTop" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".VenueActivity" />
        <activity android:name=".MatchesActivity" />
        <activity android:name=".PlayersActivity" />
        <activity android:name=".TeamsActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>